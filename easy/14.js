/**
 * @param {string[]} strs
 * @return {string}
 */
var longestCommonPrefix = function(strs) {
    for (let i=0; i < strs.length; i++){
        let currentString = strs[i];
        for(let n=0; n < currentString.length; n++){
            if(){

            } else {
                
            }
            let currentInnerString = strs[i];
        }
        
        
    }
};////////////